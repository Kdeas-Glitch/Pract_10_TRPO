<Page x:Class="Практика_7.Pages.Registration"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Практика_7.Pages"
      xmlns:validators="clr-namespace:Практика_7.ValidationRules"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      
      Title="Registration">

    <Grid>
        <Grid Width="500" Height="260" VerticalAlignment="Top" HorizontalAlignment="Left" x:Name="regForm">
            <Grid.RowDefinitions>
                <RowDefinition Height="0"/>
                <RowDefinition Height="225"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="1" Grid.ColumnSpan="2" >
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="145"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Row="0" Grid.Column="0">Имя:</Label>
                        <TextBox Name="Name" Grid.Row="0" Grid.ColumnSpan="3" Grid.Column="1" >
                            <TextBox.Text>
                                <Binding Path="Name" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <validators:RequiredRule/>
                                        <validators:OnlyLettersValidation/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                        <ItemsControl Grid.Row="0" Foreground="Red" FontSize="16" ItemsSource="{Binding ElementName=Name, Path=(Validation.Errors)}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Style="{StaticResource Validation}" Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Label Grid.Row="1" Grid.Column="0" >Фамилия:</Label>
                        <TextBox Name="LName" Grid.Row="1" Grid.ColumnSpan="3" Grid.Column="1" >
                            <TextBox.Text>
                                <Binding Path="Last_Name" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <validators:RequiredRule/>
                                        <validators:OnlyLettersValidation/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                        <ItemsControl Grid.Row="1" Foreground="Red" FontSize="16" ItemsSource="{Binding ElementName=LName, Path=(Validation.Errors)}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Style="{StaticResource Validation}" Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Label Grid.Row="2" Grid.Column="0">Отчество:</Label>
                        <TextBox Name="MName" Grid.Row="2" Grid.ColumnSpan="3" Grid.Column="1" >
                            <TextBox.Text>
                                <Binding Path="Middle_Name" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <validators:RequiredRule/>
                                        <validators:OnlyLettersValidation/>

                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                        <ItemsControl Grid.Row="2" Foreground="Red" FontSize="16" ItemsSource="{Binding ElementName=MName, Path=(Validation.Errors)}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Style="{StaticResource Validation}" Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Label Grid.Row="3" Grid.Column="0" >Специальность:</Label>
                        <TextBox  Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding Specialisation, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                        <TextBox Name="Phone" Grid.Row="3" Grid.ColumnSpan="3" Grid.Column="1" >
                            <TextBox.Text>
                                <Binding Path="Specialisation" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <validators:RequiredRule/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                        <ItemsControl Grid.Row="3" Foreground="Red" FontSize="16" ItemsSource="{Binding ElementName=Phone, Path=(Validation.Errors)}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Style="{StaticResource Validation}" Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Label Grid.Row="4" Grid.Column="0" >Пароль:</Label>
                        <TextBox Name="Pass" Grid.Row="4" Grid.ColumnSpan="3" Grid.Column="1" >
                            <TextBox.Text>
                                <Binding Path="Password" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <validators:RequiredRule/>
                                        <validators:IsNumberFloatRule/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                        <ItemsControl Grid.Row="4" Foreground="Red" FontSize="16" ItemsSource="{Binding ElementName=Pass, Path=(Validation.Errors)}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Style="{StaticResource Validation}" Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Label Grid.Row="5" Grid.Column="0" Content=" Подтвердите Пароль:"></Label>
                        <TextBox Name="Pass_Cor" Grid.Row="5" Grid.ColumnSpan="3" Grid.Column="1" >
                            <TextBox.Text>
                                <Binding Path="Cor_Password" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <validators:RequiredRule/>
                                        <validators:IsNumberFloatRule/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                        <ItemsControl Grid.Row="5" Foreground="Red" FontSize="16" ItemsSource="{Binding ElementName=Pass_Cor, Path=(Validation.Errors)}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Style="{StaticResource Validation}" Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Grid>

            </Grid>
            <StackPanel Grid.Row="3" Orientation="Horizontal" Grid.ColumnSpan="3">
                <Button  Width="150" Click="Regist" Style="{DynamicResource Add_Button}">Зарегестрироваться</Button>
                <Button  Width="150" Click="Log_in">Войти</Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
