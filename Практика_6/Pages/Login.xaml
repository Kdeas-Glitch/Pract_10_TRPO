<Page x:Class="Практика_7.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Практика_7.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">
    <Grid x:Name="Sec_Win" HorizontalAlignment="Center"  VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="165"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Margin="5" >ID:</Label>
        <TextBox Grid.Row="0" Grid.ColumnSpan="3" Grid.Column="1" Name="_ID"></TextBox>
        <Label Grid.Row="1" Grid.Column="0" Margin="5" >Пароль:</Label>
        <TextBox Grid.Row="1" Grid.ColumnSpan="3" Grid.Column="1" Name="Cor_p" Text="{Binding Cor_Password, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <Button Click="Login_Doc" Grid.Row="2" Grid.ColumnSpan="2" >Войти</Button>
        <Button  Grid.Row="3" Grid.ColumnSpan="2" Click="Regist" Style="{DynamicResource Add_Button}" >Регистрация</Button>
        <Button Grid.Row="4" Grid.ColumnSpan="2" Click="ChangeTheme" >Сменить тему</Button>
    </Grid>
</Page>
